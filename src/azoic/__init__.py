"""Azoic: scikit-learn-compatible toolkit for non-life technical tariff modelling.

Public API surfaces are added as modules land (see PROGRESS.md). Keep this file
light so `import azoic` does not pull heavy backends.
"""

__version__ = "0.4.0"
