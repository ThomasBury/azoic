"""RiskForge: scikit-learn-compatible toolkit for non-life technical tariff modelling.

Public API surfaces are added as modules land (see PROGRESS.md). Keep this file
light so `import riskforge` does not pull heavy backends.
"""

__version__ = "0.3.0"
